import SubscriptionsClient from "./subscriptions-client";

export const metadata = {
  title: "Abonnements modules — Admin",
};

export default function AdminSubscriptionsPage() {
  return <SubscriptionsClient />;
}
